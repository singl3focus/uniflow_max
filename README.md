# UniFlow MAX - Система управления задачами

UniFlow MAX - это полнофункциональная система управления задачами и учебой с поддержкой чат-бота для MAX Messenger и веб-приложения (mini-app).

## Предисловие 

Мы планировали сделать решение в виде сочетания чат-бота и мини-приложения. Однако при заполнении анкеты можно было выбрать лишь одно, но при этом мы реализовали два готовых варианта решения.

**Мы выбрали: чат бот**

> НО! Вы также можете локально запустить фронтенд и он будет использовать локально поднятный бекенд. Мы уточняем это, чтобы подчеркнуть, что кроме чат-бот у нас есть полностью работающее мини-приложение

## 📋 Возможности

### Чат-бот
- 📅 Просмотр задач по датам с навигацией (вчера/сегодня/завтра)
- 📥 Входящие задачи (без контекста) с быстрым завершением
- 📁 Управление контекстами (учеба, работа, проекты)
- ➕ Создание задач с выбором даты и контекста
- 🔍 Поиск по задачам и контекстам
- ✅ Быстрое завершение задач
- 💬 Интерактивные клавиатуры

### Веб-приложение (Mini-App)
- Полный CRUD для задач и контекстов
- Календарь и расписание
- Аутентификация через MAX
- Адаптивный интерфейс

## 🚀 Быстрый старт

### Требования

- Docker и Docker Compose
- Go 1.24+ (для локальной разработки)
- PostgreSQL 16+ (запускается в Docker)
- MAX Bot Token (получить через @MaxDevBot). Наш токен: <спрятан> (т.к. репозиторий публичный)

### 1. Наш бот

@t645_hakaton_bot

### 2. Настройка конфигурации

Скопируйте пример конфигурации и укажите токен бота:

```bash
cd backend

# Для локального запуска
cp config.env.example config.env

# Для Docker запуска
cp config.env.example docker/configs/config.env
```

Откройте нужный файл и замените `your-max-bot-token-here` на реальный токен:

```env
MAX_BOT_TOKEN=ваш-реальный-токен-от-MaxDevBot
```

### 3. Запуск через Docker Compose (рекомендуется)

```bash
# Перейдите в папку backend
cd backend

# Соберите и запустите все сервисы
docker-compose up -d --build

# Проверьте статус контейнеров
docker-compose ps

# Просмотр логов
docker-compose logs -f app
```

Это запустит:
- PostgreSQL базу данных на порту 50040
- Бэкенд API на порту 50031
- Автоматические миграции БД
- Long polling для получения обновлений от MAX

### 4. Запуск локально (без Docker)

#### Шаг 1: Запустите PostgreSQL

```bash
# Через Docker
docker run -d \
  --name uniflow-postgres \
  -e POSTGRES_PASSWORD=postgres \
  -e POSTGRES_DB=uniflow \
  -p 50040:5432 \
  postgres:16-alpine

# Или используйте существующий PostgreSQL и измените PG_DSN в config.env
```

#### Шаг 2: Примените миграции

```bash
cd backend

# Установите goose (если не установлен)
go install github.com/pressly/goose/v3/cmd/goose@latest

# Примените миграции
goose -dir migrations postgres "host=localhost port=50040 dbname=uniflow user=postgres password=postgres sslmode=disable" up
```

#### Шаг 3: Соберите и запустите бэкенд

```bash
cd backend

# Установите зависимости
go mod download

# Соберите приложение
go build -o bin/server ./cmd/project

# Запустите
./bin/server
```

Или в одну команду:

```bash
cd backend
go run ./cmd/project/main.go
```

### 5. Проверка работы

После запуска:

1. **Откройте MAX Messenger**
2. **Найдите своего бота** по имени, которое вы указали при создании
3. **Нажмите "Запустить"** или отправьте `/start`
4. **Используйте команды:**
   - `/today` - задачи на сегодня
   - `/newtask` - создать задачу
   - `/contexts` - просмотр контекстов
   - `/help` - справка

Бот работает через **Long Polling**, поэтому webhook не требуется!

## 📦 Структура проекта

```
uniflow_max/
├── backend/              # Go backend
│   ├── cmd/
│   │   └── project/
│   │       └── main.go   # Точка входа
│   ├── internal/
│   │   ├── adapters/
│   │   │   ├── http/     # REST API handlers
│   │   │   ├── max/      # MAX bot integration
│   │   │   ├── postgres/ # Database repositories
│   │   │   └── redis/    # Redis cache (опционально)
│   │   └── core/
│   │       ├── models/   # Доменные модели
│   │       ├── ports/    # Интерфейсы
│   │       └── usecase/  # Бизнес-логика
│   ├── migrations/       # SQL миграции
│   ├── config.env        # Конфигурация (создать из .example)
│   ├── docker-compose.yaml
│   └── go.mod
├── frontend/             # React frontend (mini-app)
│   ├── src/
│   │   ├── components/
│   │   ├── pages/
│   │   └── lib/
│   └── package.json
└── README.md
```

## 🛠 Полезные команды

### Docker

```bash
# Остановить все контейнеры
docker-compose down

# Пересобрать и перезапустить
docker-compose up -d --build

# Посмотреть логи
docker-compose logs -f app

# Войти в контейнер
docker exec -it uniflow-app sh

# Очистить все (включая данные БД)
docker-compose down -v
```

### Миграции

```bash
# Применить все миграции
goose -dir migrations postgres "$PG_DSN" up

# Откатить последнюю миграцию
goose -dir migrations postgres "$PG_DSN" down

# Создать новую миграцию
goose -dir migrations create migration_name sql
```

### Разработка

```bash
# Запустить с hot-reload (требует air)
go install github.com/cosmtrek/air@latest
air

# Запустить тесты
go test ./...

# Проверить код линтером
golangci-lint run

# Форматирование кода
go fmt ./...
```

## 🔧 Конфигурация

### Основные параметры (config.env)

```env
# HTTP Server
HTTP_PORT=50031

# Database
PG_DSN="host=localhost port=50040 dbname=uniflow user=postgres password=postgres sslmode=disable"

# MAX Bot
MAX_BOT_TOKEN=your-max-bot-token-here  # ОБЯЗАТЕЛЬНО замените!
MAX_WEBHOOK_URL=                        # Пусто = Long Polling

# JWT
JWT_SECRET=your-secret-key-change-in-production
```

### Long Polling vs Webhook

**Long Polling (по умолчанию):**
- Бот сам запрашивает обновления у MAX
- Не требует публичного HTTPS URL
- Идеально для разработки и тестирования
- Работает сразу после запуска

**Webhook (для production):**
- MAX отправляет обновления на ваш сервер
- Требует HTTPS URL (например, через ngrok или VPS)
- Более эффективно для продакшена
- Установите `MAX_WEBHOOK_URL=https://yourdomain.com/max/webhook`

## 📱 API Endpoints

```
GET  /health           - Проверка здоровья
GET  /api/v1/tasks     - Список задач
POST /api/v1/tasks     - Создать задачу
GET  /api/v1/contexts  - Список контекстов
POST /api/v1/contexts  - Создать контекст
POST /max/webhook      - Webhook для MAX (если используется)
```

## 🐛 Отладка

### Проблема: Бот не отвечает

1. Проверьте логи: `docker-compose logs -f app`
2. Убедитесь, что токен правильный
3. Проверьте, что бэкенд запущен: `curl http://localhost:50031/health`
4. Проверьте подключение к БД

### Проблема: Ошибки миграций

```bash
# Сбросьте БД и примените заново
docker-compose down -v
docker-compose up -d
```

### Проблема: Изменения не применяются

```bash
# Пересоберите контейнеры
docker-compose up -d --build --force-recreate
```

## 📚 Документация

- [MAX Bot API Documentation](https://dev.max.ru/docs/bot-api)
- [API форматы](./API_FORMATS.md)
- [Структура меню бота](./backend/docs/BOT_MENU_STRUCTURE.md)
- [Интеграция с MAX](./backend/docs/MAX_INTEGRATION.md)

## 🤝 Команды бота

### Основные
- `/start` - Приветствие и меню
- `/menu` - Главное меню
- `/help` - Справка

### Задачи
- `/today` - Задачи на сегодня
- `/tasks` - Все задачи
- `/newtask` - Создать задачу
- `/search <запрос>` - Поиск

### Контексты
- `/contexts` - Все контексты
- `/newcontext` - Создать контекст

### Служебные
- `/cancel` - Отменить действие

## 🏗 Архитектура

Проект использует **Clean Architecture**:
- **Handlers** (adapters/http, adapters/max) - входные адаптеры
- **Usecase** (core/usecase) - бизнес-логика
- **Repositories** (adapters/postgres) - работа с БД
- **Models** (core/models) - доменные модели

## 📝 Лицензия

MIT

## 👨‍💻 Автор

UniFlow MAX Bot - система управления задачами для студентов

---

**Важно:** Не забудьте заменить `your-max-bot-token-here` на реальный токен перед запуском!
