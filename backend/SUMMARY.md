# UniFlow - Сводка проекта

## Общая информация

**Название:** UniFlow  
**Описание:** Персональный помощник по продуктивности с интеграцией MAX Messenger  
**Тип:** Modular Monolith с Clean Architecture  
**Версия Go:** 1.23  
**База данных:** PostgreSQL  

## Реализованные возможности

### ✅ Полностью реализовано

#### 1. Архитектура проекта
- Clean Architecture с разделением на слои
- Модульная монолитная архитектура
- Dependency injection через конструкторы

#### 2. Модели данных (Domain Layer)
- `User` - пользователи с MAX ID
- `Context` - контексты для группировки задач
- `Task` - задачи с статусами и дедлайнами
- `ScheduleEntry` - расписание
- `Notification` - уведомления
- `Note` - заметки
- `FocusSession` - фокус-сессии

#### 3. База данных
- Схема PostgreSQL с 7 таблицами
- Индексы для оптимизации запросов
- Миграции в `migrations/schema.sql`

#### 4. Репозитории (Infrastructure Layer)
- Интерфейсы в `internal/core/ports/repository`
- PostgreSQL реализации:
  - `UserRepository` - полная реализация
  - `ContextRepository` - полная реализация
  - `TaskRepository` - полная реализация
  - Остальные - заглушки для будущей реализации

#### 5. Use Cases (Application Layer)
- Управление пользователями:
  - `GetOrCreateUserByMaxID` - получение/создание пользователя по MAX ID
- Управление контекстами:
  - `CreateContext` - создание контекста
  - `GetUserContexts` - получение всех контекстов пользователя
- Управление задачами:
  - `CreateTask` - создание задачи
  - `UpdateTask` - обновление задачи
  - `UpdateTaskStatus` - изменение статуса
  - `GetUserTasks` - получение задач пользователя
  - `GetTasksDueToday` - задачи на сегодня

#### 6. HTTP API (Transport Layer)
- Chi router v5
- Обработчики:
  - `AuthHandler` - авторизация через MAX
  - `ContextHandler` - CRUD операции с контекстами
  - `TaskHandler` - CRUD операции с задачами
- Middleware для логирования
- Структурированные JSON ответы

#### 7. Интеграция с MAX Messenger
- **Client** (`internal/adapters/max/client.go`):
  - Отправка сообщений пользователям
  - Отправка сообщений в чаты
  - Настройка webhook
  - Long polling для получения обновлений
  
- **WebhookHandler** (`internal/adapters/max/webhook.go`):
  - Обработка входящих webhook запросов
  - Поддержка различных типов обновлений
  - Расширяемая архитектура обработчиков
  
- **NotificationService** (`internal/adapters/max/notification.go`):
  - Напоминания о задачах
  - Ежедневные сводки
  - Уведомления о фокус-сессиях
  - Произвольные уведомления

#### 8. Конфигурация
- Поддержка переменных окружения
- Файл конфигурации `config.env`
- Параметры:
  - PostgreSQL DSN
  - HTTP порт
  - MAX Bot Token
  - MAX Webhook URL
  - Уровень логирования

#### 9. Логирование
- Structured logging с zerolog
- Контекстное логирование
- Настраиваемый уровень логов

### 🚧 Частично реализовано

#### 1. Репозитории
- Schedule, Notification, Note, FocusSession - имеют заглушки
- Требуется полная реализация SQL запросов

#### 2. Use Cases
- Основные операции реализованы
- Расширенная логика (напоминания, планирование) - требуется доработка

#### 3. HTTP Handlers
- Базовые CRUD операции готовы
- Требуется добавить обработку ошибок и валидацию

### ❌ Не реализовано

#### 1. Функциональность
- Автоматические напоминания
- Планировщик задач
- Статистика и аналитика
- Экспорт данных

#### 2. Тестирование
- Unit тесты
- Integration тесты
- E2E тесты

#### 3. Документация API
- OpenAPI/Swagger спецификация частично готова
- Требуется актуализация

#### 4. Безопасность
- Валидация входных данных
- Rate limiting
- CORS настройки

#### 5. DevOps
- CI/CD pipeline
- Docker Compose для разработки
- Kubernetes манифесты

## Структура проекта

```
uniflow_max/
├── api/                      # API документация
│   ├── docs.md
│   └── http/v1/openapi.yaml
├── build/docker/            # Docker конфигурация
│   ├── configs/config.env
│   └── ...
├── cmd/project/             # Точка входа
│   └── main.go
├── config/                  # Конфигурация
│   ├── config.go
│   ├── env/env.go
│   └── flags/flags.go
├── docs/                    # Документация
│   ├── easyp.md
│   └── MAX_INTEGRATION.md
├── internal/
│   ├── adapters/           # Infrastructure Layer
│   │   ├── http/          # HTTP transport
│   │   ├── max/           # MAX integration
│   │   ├── postgres/      # Database
│   │   └── redis/         # Cache (заглушка)
│   ├── app/               # Application setup
│   └── core/              # Domain + Application
│       ├── models/        # Domain entities
│       ├── ports/         # Interfaces
│       └── usecase/       # Business logic
├── migrations/             # Database migrations
│   └── schema.sql
├── pkg/                    # Общие пакеты
│   ├── errs/              # Error handling
│   └── logger/            # Logging
├── go.mod
├── go.sum
└── README.md
```

## Зависимости

### Основные
- `github.com/go-chi/chi/v5` - HTTP router
- `github.com/jackc/pgx/v5` - PostgreSQL driver
- `github.com/Masterminds/squirrel` - SQL query builder
- `github.com/max-messenger/max-bot-api-client-go` - MAX Bot API
- `github.com/google/uuid` - UUID generation
- `github.com/rs/zerolog` - Structured logging

### Вспомогательные
- `github.com/caarlos0/env/v6` - Environment variables
- `github.com/kr/pretty` - Pretty printing

## Быстрый старт

### 1. Настройка окружения

```bash
cp config.env.example build/docker/configs/config.env
# Отредактируйте config.env с вашими параметрами
```

### 2. Создание базы данных

```bash
psql -U postgres -f migrations/schema.sql
```

### 3. Конфигурация MAX бота

1. Создайте бота через MasterBot в MAX
2. Скопируйте токен в `MAX_BOT_TOKEN`
3. Настройте webhook URL (опционально)

### 4. Запуск приложения

```bash
cd cmd/project
go run main.go
```

Сервер запустится на порту, указанном в `HTTP_PORT` (по умолчанию 8080).

## API Endpoints

### Пользователи
- `POST /api/v1/auth/max` - Авторизация через MAX

### Контексты
- `GET /api/v1/contexts` - Получить все контексты
- `POST /api/v1/contexts` - Создать контекст
- `GET /api/v1/contexts/:id` - Получить контекст
- `PUT /api/v1/contexts/:id` - Обновить контекст
- `DELETE /api/v1/contexts/:id` - Удалить контекст

### Задачи
- `GET /api/v1/tasks` - Получить все задачи
- `GET /api/v1/tasks/today` - Задачи на сегодня
- `POST /api/v1/tasks` - Создать задачу
- `GET /api/v1/tasks/:id` - Получить задачу
- `PUT /api/v1/tasks/:id` - Обновить задачу
- `PATCH /api/v1/tasks/:id/status` - Изменить статус
- `DELETE /api/v1/tasks/:id` - Удалить задачу

### MAX Webhook
- `POST /webhook/max` - Webhook для MAX обновлений

## Следующие шаги

### Приоритет 1 (Критично)
1. Реализовать оставшиеся репозитории (Schedule, Notification, Note, FocusSession)
2. Добавить валидацию входных данных
3. Улучшить обработку ошибок
4. Написать unit тесты для use cases

### Приоритет 2 (Важно)
1. Реализовать автоматические напоминания
2. Добавить планировщик для периодических задач
3. Создать интерактивные команды для MAX бота
4. Добавить middleware для аутентификации

### Приоритет 3 (Желательно)
1. Статистика и аналитика
2. Экспорт/импорт данных
3. Интеграция с другими сервисами
4. Web UI для администрирования

## Известные ограничения

1. **Аутентификация**: Базовая проверка по MAX User ID, нет полноценной JWT аутентификации
2. **Производительность**: Нет кэширования, все запросы идут в БД
3. **Масштабирование**: Нет поддержки горизонтального масштабирования
4. **Resilience**: Нет retry механизмов и circuit breakers

## Контакты и ресурсы

- **Документация MAX API**: https://dev.max.ru/
- **MAX Bot SDK**: https://github.com/max-messenger/max-bot-api-client-go
- **Clean Architecture**: https://blog.cleancoder.com/uncle-bob/2012/08/13/the-clean-architecture.html

## Лицензия

MIT License
