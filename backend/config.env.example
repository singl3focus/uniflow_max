# Application Configuration
HTTP_PORT=50031
LOGGER_LEVEL=info

# PostgreSQL Configuration (for local development)
PG_DSN="host=localhost port=5432 dbname=auth user=postgres password=postgres sslmode=disable"

# PostgreSQL Configuration (for Docker Compose)
PG_DATABASE_NAME=auth
PG_USER=postgres
PG_PASSWORD=postgres
PG_EXTENDED_PORT=50040

# JWT Configuration
JWT_SECRET=your-secret-key-change-in-production

# MAX Messenger Configuration
# Для локальной разработки оставьте MAX_WEBHOOK_URL пустым - будет использован long polling
# Для продакшена укажите публичный HTTPS URL (например, https://yourdomain.com/max/webhook)
MAX_BOT_TOKEN=your-max-bot-token-here
MAX_WEBHOOK_URL=

# Migrations Configuration
MIGRATIONS_DIR=migrations
